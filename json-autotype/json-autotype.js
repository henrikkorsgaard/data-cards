exports.analyseJSON = function(json){

    console.log(json.float)

    return false
}

function recursiveAnalysis(data){
    if(data.isArray()){

    } else {
        for( var key in data){
            //we want to test the value
        }
    }
    //test if array -> then we want to iterate

    //test if key / value
}
